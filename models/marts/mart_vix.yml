version: 2

models:
  - name: mart_vix
    columns:
      - name: event_time
        tests:
          - unique
          - not_null
      - name: signal
        tests:
          - accepted_values:
              arguments:
                values: ['BULLISH', 'NEUTRAL', 'BEARISH']
